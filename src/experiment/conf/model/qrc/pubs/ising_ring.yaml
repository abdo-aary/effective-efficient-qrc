family: ising_ring_swap
angle_positioning: tanh
window_length: ${data.sampling.w}

# kwargs forwarded to CircuitFactory.create_pubs_dataset_reservoirs_IsingRingSWAP
lam_0: 0.1
num_reservoirs: 3
seed: ${seed}
eps: 1e-8
projection_backend: cupy
projection_device: 0
dynamics_mode: random
