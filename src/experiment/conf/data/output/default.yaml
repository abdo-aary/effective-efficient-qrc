root_dir: ${oc.env:PROJECT_ROOT}
save_dir: ${.root_dir}/storage/data/synthetic/${process.kind}/N=${sampling.N}__w=${sampling.w}__d=${sampling.d}__s=${sampling.s}

name: auto
format: npz
overwrite: false
save_meta: true
save_config: true
